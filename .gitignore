# Ignore the plug.vim plugin
autoload/plug.vim
# Ignore the directory of the plugins downloaded by plug.vim
plugged/*
# Ignore spell directory
spell/*

# Ignore the undo-directory content
undodir/*
# But the directory itself and a placeholder
!undodir/.git_keep
